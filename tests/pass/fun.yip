identity = fun(x) x end
negate = fun(x) -x end

addtwo = fun(x, y) x + y end
addthreee = fun(x, y, z) x + y + z end

addn = fun(a, b, c, d, e, f, g, h) 7 end

main = fun (x)  print(x) end

higher = fun   (x) print(negate(y))                end
